try {    var multiplier = eval(prompt ("How many times bigger or smaller is the scale?", 2, "Multiplier"));    //take every peice of text and double the size and its line spacing.    for ( i = 0; i < app.activeDocument.textFrames.length; i++ ) {        text = app.activeDocument.textFrames[i].textRange;        mixedSize = false;        for ( j = 0; j < text.characters.length; j++ ) {           aChar = text.characters[j];           aChar.size = aChar.size * multiplier;           if (aChar.autoLeading == false) {                aChar.leading = aChar.leading * multiplier;           }        }    }    //take every path and double its stoke width and dashing.    for ( i = 0; i < app.activeDocument.pathItems.length; i++ ) {        aPath = app.activeDocument.pathItems[i];        if (aPath.stroked) {            aPath.strokeWidth = aPath.strokeWidth * multiplier;            if (aPath.strokeDashes.length > 0) {                newDash = [];                for ( j = 0; j < aPath.strokeDashes.length; j++) {                    newDash.push(aPath.strokeDashes[j] * multiplier);                }                aPath.strokeDashes = newDash;            }        }    }    alert("note that this script will not handle multiple strokes or strokes on text. Please find and correct these by hand.");}catch (e){	alert("Script Failed!\n"+e);}