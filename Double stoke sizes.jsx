try {//take every path and double its stoke width and dashing.    for ( i = 0; i < app.activeDocument.pathItems.length; i++ ) {        aPath = app.activeDocument.pathItems[i];        if (aPath.stroked) {            aPath.strokeWidth = aPath.strokeWidth * 4;            if (aPath.strokeDashes.length > 0) {                newDash = [];                for ( j = 0; j < aPath.strokeDashes.length; j++) {                    newDash.push(aPath.strokeDashes[j] * 4);                }                aPath.strokeDashes = newDash;            }        }    }}catch (e){	alert("Script Failed!\n"+e);}