try{	// Check current document for textFrames.	if ( app.documents.length < 1 ) {		alert ( "open a document with text objects and select them." );	}	else {		docRef = app.activeDocument;		if ( docRef.textFrames.length < 1 ) {			alert ( "Select some text objects." );		}		//where text fames are selected swap the jusification		for ( i = 0; i < app.activeDocument.textFrames.length; i++ ) {            text = app.activeDocument.textFrames[i].textRange;            mixedSize = false;            for ( j = 0; j < text.characters.length; j++ ) {               aChar = text.characters[j];               if (aChar.size != text.size) {                   aChar.size = aChar.size * 2;                   mixedSize = true;               }               if (aChar.autoLeading == false) {                    aChar.leading = aChar.leading * 2;               }            }            if (mixedSize == false) {                text.size = text.size * 2;            }        }	}	 	 		}catch (e){	alert("Script Failed!\n"+e);}